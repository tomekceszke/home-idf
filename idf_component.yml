version: "0.1.1"
description: Shared ESP-IDF framework for home controllers (WiFi, logging, NTP, OTA, health/rollback, ntfy, web auth and API guards)
url: https://github.com/tomekceszke/home-idf
license: MIT
targets:
  - esp32
dependencies:
  idf: ">=5.4"
files:
  exclude:
    - "examples/**/build/**"
    - "examples/**/managed_components/**"
